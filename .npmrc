
# Configuration for package management

# We want optional dependencies for esbuild to work properly
optional=true
fund=false
audit=false

# Set legacy peer deps to true for compatibility
legacy-peer-deps=true
strict-peer-dependencies=false

# We need to run scripts for esbuild installation
ignore-scripts=false

# Don't fail on missing optional dependencies
omit=optional

# Explicitly set mock versions for platform-specific dependencies
@rollup/rollup-win32-x64-msvc=0.0.0
@rollup/rollup-linux-x64-gnu=0.0.0
@rollup/rollup-darwin-x64=0.0.0
@rollup/rollup-darwin-arm64=0.0.0

# But force esbuild and its platform-specific dependencies
@esbuild/linux-x64=latest
