
# Explicitly ignore optional dependencies to prevent platform-specific issues
optional=false
omit=optional
# Disable audit to speed up installation
audit=false
# Don't save exact versions to avoid platform-specific issues
save-exact=false
# Force using the latest version of packages
force=true
